---
import BlogIndex from '../../../components/pages/BlogIndex.astro';

export function getStaticPaths() {
  return [
    { params: { lang: 'pt-br' } },
  ];
}

const { lang } = Astro.params;
---

<BlogIndex lang={lang as 'en' | 'pt-br'} />
