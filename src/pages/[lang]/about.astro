---
import AboutPage from '../../components/pages/AboutPage.astro';

export function getStaticPaths() {
  return [
    { params: { lang: 'pt-br' } },
  ];
}

const { lang } = Astro.params;
---

<AboutPage lang={lang as 'en' | 'pt-br'} />
